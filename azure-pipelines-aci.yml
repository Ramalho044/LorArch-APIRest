trigger:
  branches:
    include:
      - master
      - main

pr:
  branches:
    include:
      - master
      - main

pool:
  vmImage: 'ubuntu-latest'

name: $(Date:yyyyMMdd).$(Rev:r)

variables:
  # ================== GERAIS ==================
  appName: 'lorarch-app'
  resourceGroup: 'LorArch'
  location: 'eastus'

  # ================== ACR =====================
  acrName: 'acrlorch'
  acrLoginServer: 'acrlorch.azurecr.io'
  imageRepository: 'fiap/lorarch'
  dockerRegistryServiceConnection: 'sc-acr-lorch'

  # ================== AZURE SUBSCRIPTION ======
  azureServiceConnection: 'sc-azure-lorch'

  # IMPORTANTE:
  # DB_URL, DB_USER, DB_PASSWORD, ACR_USER, ACR_PASS
  # DEVEM SER CRIADAS COMO "Pipeline variables" (secret) NO DEVOPS

stages:
# ========================================================
#  STAGE 1 - CONTÍNUOUS INTEGRATION (BUILD + TEST + PUSH)
# ========================================================
- stage: CI
  displayName: 'CI — Build, Test (sem travar), Docker build & Push'
  jobs:
    - job: build
      displayName: 'Job: Build & Test Java'
      steps:
        # ---------------------------------------
        # Checkout do repositório
        # ---------------------------------------
        - checkout: self
          fetchDepth: 1

        # ---------------------------------------
        # Java 21
        # ---------------------------------------
        - task: JavaToolInstaller@0
          displayName: 'Instalar Java 21'
          inputs:
            versionSpec: '21'
            jdkArchitectureOption: 'x64'
            jdkSourceOption: 'PreInstalled'

        # ---------------------------------------
        # Cache Gradle
        # ---------------------------------------
        - task: Cache@2
          displayName: 'Cache Gradle'
          inputs:
            key: 'gradle | "$(Agent.OS)" | **/gradle-wrapper.properties'
            restoreKeys: |
              gradle | "$(Agent.OS)"
            path: ~/.gradle

        # ---------------------------------------
        # Subir SQL Server local p/ testes (CI)
        # ---------------------------------------
        - bash: |
            set -euo pipefail

            echo "Removendo container antigo (se existir)..."
            docker rm -f mssql_ci 2>/dev/null || true

            echo "Subindo SQL Server 2022 para testes..."
            docker run -d --name mssql_ci \
              -e 'ACCEPT_EULA=Y' \
              -e 'MSSQL_SA_PASSWORD=Str0ng_Passw0rd!' \
              -p 1433:1433 \
              mcr.microsoft.com/mssql/server:2022-latest

            echo "Esperando SQL iniciar..."
            for i in {1..30}; do
              if docker logs mssql_ci 2>&1 | grep -q "SQL Server is now ready for client connections"; then
                echo "SQL pronto."
                break
              fi
              echo "Aguardando... $i"
              sleep 5
            done

            echo "Criando banco lorarch_ci (se não existir)..."
            docker exec mssql_ci /opt/mssql-tools/bin/sqlcmd \
              -S localhost -U sa -P 'Str0ng_Passw0rd!' \
              -Q "IF DB_ID('lorarch_ci') IS NULL CREATE DATABASE lorarch_ci;"
          displayName: 'Subir SQL Server (CI) — com espera'

        # ---------------------------------------
        # Testes + Jacoco (NÃO quebram o pipeline)
        # ---------------------------------------
        - bash: |
            set -e

            echo "Rodando testes (se falhar, não mata o build)..."
            ./gradlew clean test jacocoTestReport --no-daemon || true
          displayName: 'Rodar Testes + Jacoco (não falha build)'

        # ---------------------------------------
        # Publicar resultados de teste
        # ---------------------------------------
        - task: PublishTestResults@2
          displayName: 'Publicar resultados de teste'
          inputs:
            testResultsFormat: 'JUnit'
            testResultsFiles: '**/TEST-*.xml'
            searchFolder: '$(System.DefaultWorkingDirectory)'
            failTaskOnFailedTests: false

        # ---------------------------------------
        # Publicar relatório Jacoco
        # ---------------------------------------
        - task: PublishCodeCoverageResults@2
          displayName: 'Publicar relatório Jacoco'
          inputs:
            codeCoverageTool: 'JaCoCo'
            summaryFileLocation: '$(System.DefaultWorkingDirectory)/**/build/reports/jacoco/test/jacocoTestReport.xml'
            reportDirectory: '$(System.DefaultWorkingDirectory)/**/build/reports/jacoco/test/html'
            failIfCoverageEmpty: false

        # ---------------------------------------
        # Gerar artefato JAR
        # ---------------------------------------
        - bash: |
            set -e
            ./gradlew bootJar --no-daemon

            JAR=$(ls build/libs/*SNAPSHOT.jar | head -n1)
            echo "JAR gerado: $JAR"

            mkdir -p drop
            cp "$JAR" drop/app.jar
          displayName: 'Gerar artefato JAR'

        - task: PublishBuildArtifacts@1
          displayName: 'Publicar artefato (drop)'
          inputs:
            pathToPublish: 'drop'
            artifactName: 'drop'

        # ---------------------------------------
        # Build & Push Docker pro ACR
        # ---------------------------------------
        - task: Docker@2
          displayName: 'Build & Push da imagem Docker → ACR'
          inputs:
            containerRegistry: '$(dockerRegistryServiceConnection)'
            repository: '$(imageRepository)'
            command: 'buildAndPush'
            Dockerfile: 'Dockerfile'
            buildContext: '$(System.DefaultWorkingDirectory)'
            tags: |
              $(Build.BuildId)
              latest

# ========================================================
#  STAGE 2 - CONTINUOUS DEPLOYMENT (AZURE ACI)
# ========================================================
- stage: CD
  displayName: 'CD — Deploy no Azure Container Instance (ACI)'
  dependsOn: CI
  condition: succeeded()

  jobs:
    - job: deploy
      displayName: 'Job: Deploy ACI'
      steps:
        - task: AzureCLI@2
          displayName: 'Criar / Atualizar Container no ACI'
          inputs:
            azureSubscription: '$(azureServiceConnection)'
            scriptType: bash
            scriptLocation: inlineScript
            inlineScript: |
              set -e

              IMAGE="$(acrLoginServer)/$(imageRepository):$(Build.BuildId)"
              DNS_LABEL="$(echo $(appName) | tr '[:upper:]' '[:lower:]')"

              echo "Usando imagem: ${IMAGE}"
              echo "DNS label: ${DNS_LABEL}"

              echo "Logando no ACR..."
              az acr login --name $(acrName)

              echo "Removendo container antigo (se existir)..."
              if az container show -g $(resourceGroup) -n $(appName) &>/dev/null; then
                az container delete -g $(resourceGroup) -n $(appName) --yes
              fi

              echo "Criando container no ACI..."
              az container create \
                --name $(appName) \
                --resource-group $(resourceGroup) \
                --location $(location) \
                --image ${IMAGE} \
                --os-type Linux \
                --cpu 1 \
                --memory 1.5 \
                --restart-policy Always \
                --ports 8080 \
                --dns-name-label ${DNS_LABEL} \
                --registry-login-server $(acrLoginServer) \
                --registry-username $(ACR_USER) \
                --registry-password $(ACR_PASS) \
                --environment-variables \
                    SPRING_PROFILES_ACTIVE=prod \
                    SERVER_PORT=8080 \
                --secure-environment-variables \
                    SPRING_DATASOURCE_URL=$(DB_URL) \
                    SPRING_DATASOURCE_USERNAME=$(DB_USER) \
                    SPRING_DATASOURCE_PASSWORD=$(DB_PASSWORD)

              echo "Aguardando container ficar 'Running'..."
              for i in {1..40}; do
                STATE=$(az container show -g $(resourceGroup) -n $(appName) --query "instanceView.state" -o tsv)
                echo "Estado: $STATE"
                [ "$STATE" = "Running" ] && break
                sleep 6
              done

              FQDN=$(az container show -g $(resourceGroup) -n $(appName) --query "ipAddress.fqdn" -o tsv)
              echo "Aplicação deve responder em: http://$FQDN:8080"

              echo "Health-check /actuator/health..."
              for i in {1..30}; do
                code=$(curl -s -o /dev/null -w "%{http_code}" "http://$FQDN:8080/actuator/health" || true)
                echo "Tentativa $i -> HTTP $code"
                [ "$code" = "200" ] && echo "Health OK!" && exit 0
                sleep 5
              done

              echo "Logs do container (últimos 100):"
              az container logs -g $(resourceGroup) -n $(appName) --tail 100 || true
              exit 1
